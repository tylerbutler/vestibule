## v0.1.0 - 2026-08-31

### Breaking

- `parse_primary_email` now returns `Result(Option(String), AuthError(e))` instead of `Option(String)`, distinguishing an unparseable emails response from the absence of a primary verified email

### Added

- Expose separate OAuth request builders and response parsers for sans-I/O integrations

### Changed

- Repositioned as demo-ready auth: not security audited, not for production use

### Dependencies

- Switch test framework from startest to gleeunit
- Updated vestibule to 0.1.0
