package = "vestibule_apple"
kind = "Changed"
body = "Apple Sign In now carries `id_token` through `strategy.exchange_artifacts` instead of an implicit ID-token cache. `AppleCache` now only manages JWKS caching, and the `vestibule_apple/id_token_cache` module has been removed."
