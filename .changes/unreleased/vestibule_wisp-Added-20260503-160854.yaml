project: vestibule_wisp
kind: Added
body: Wisp middleware for OAuth callback handling, with an opaque ETS-backed `StateStore`, server-side OAuth session TTLs, configurable cookie options, fail-closed malformed POST callback parsing, and compatibility with the current `vestibule` auth error and strategy contracts.
