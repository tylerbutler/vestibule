package = "vestibule"
kind = "Security"
body = "Document secret source-to-sink lifecycles; redact authorization and exchange artifacts; filter sensitive log-field aliases; and prevent provider-controlled response fields or endpoint URLs from entering public errors."
