---
title: "vestibule/config"
description: "OAuth client configuration, authentication methods, and per-request authorization options."
nav:
  group: Reference
  groupOrder: 20
  order: 13
  label: "vestibule/config"
toc:
  - href: "#types"
    label: "Types"
  - href: "#functions"
    label: "Functions"
searchTerms:
  - api
  - reference
  - module
  - vestibule/config
---

# `vestibule/config`

OAuth client configuration. `ClientConfig` carries the durable client ID,
redirect URI, and authentication method. `AuthorizeOptions` carries
per-request scopes and extra authorization parameters.

## Types

### `ClientAuth`

OAuth client authentication method.

```gleam
pub type ClientAuth {
  PublicClient
  ClientSecret(String)
  ClientAssertion(String)
}
```

### `ClientConfig`

Durable OAuth client configuration.

```gleam
pub type ClientConfig
```

### `AuthorizeOptions`

Per-authorization request options.

```gleam
pub type AuthorizeOptions
```

## Functions

### `client_id`

Return the configured OAuth client ID.

```gleam
pub fn client_id(ClientConfig) -> String
```

### `client_auth`

Return the configured OAuth client authentication method.

```gleam
pub fn client_auth(ClientConfig) -> ClientAuth
```

### `client_secret`

Return a client secret value when the authentication method provides one.

```gleam
pub fn client_secret(ClientConfig) -> Result(String, error.AuthError(a))
```

### `extra_params`

Return configured extra authorization query parameters.

```gleam
pub fn extra_params(AuthorizeOptions) -> dict.Dict(String, String)
```

### `new`

Create durable client configuration.

```gleam
pub fn new(
  client_id: String,
  redirect_uri: String,
  auth: ClientAuth
) -> ClientConfig
```

### `redirect_uri`

Return the redirect URI registered with the provider.

```gleam
pub fn redirect_uri(ClientConfig) -> String
```

### `scopes`

Return configured per-request scopes (empty list means use strategy defaults).

```gleam
pub fn scopes(AuthorizeOptions) -> List(String)
```

### `with_extra_params`

Add extra query parameters to an authorization request.

Reserved OAuth authorization parameters are rejected so callers cannot
override values generated by Vestibule or provider strategies.

```gleam
pub fn with_extra_params(
  AuthorizeOptions,
  List(#(String, String))
) -> Result(AuthorizeOptions, error.AuthError(a))
```

### `with_scopes`

Set custom scopes for an authorization request, replacing any defaults.

```gleam
pub fn with_scopes(
  AuthorizeOptions,
  List(String)
) -> AuthorizeOptions
```

### `authorize_options`

Create empty per-authorization request options.

```gleam
pub fn authorize_options() -> AuthorizeOptions
```
